#!/usr/bin/python

import sys

def main():
	total = 0.0
	n = 0.0
	while 1:
		line = sys.stdin.readline()
		if not line:
			break

		val = float(line)
		n = n+1.0
		total = total+val

	print total/n
main()
